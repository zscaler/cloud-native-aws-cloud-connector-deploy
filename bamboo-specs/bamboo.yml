---
version: 2
plan:
  project-key: IC
  key: VER
  name: Cloud Native AWS Cloud Connector Deploy CI
stages:
  - Verify cfn:
    - Verify

Verify:
  tasks:
    - script: | 
        #!/bin/bash
        trufflehog git --no-update --only-verified file://.//
    - script: | 
        #!/usr/bin/env bash
        set -eo
        cfn-lint cloudformation-templates/**/*.yaml

